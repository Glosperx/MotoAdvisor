# Copy to .env and fill in real values before running docker-compose up.
# Never commit .env to git.

# ── Database ────────────────────────────────────────────────────────────────
# External PostgreSQL (no DB container).
# Format: Host=<host>;Port=5432;Database=motoadvisor;Username=<user>;Password=<pass>;Ssl Mode=Require
DB_CONNECTION_STRING=Host=your-db-host;Port=5432;Database=motoadvisor;Username=motoadvisor_user;Password=changeme;Ssl Mode=Require

# ── JWT ─────────────────────────────────────────────────────────────────────
# Must be at least 32 characters. Use: openssl rand -base64 48
JWT_KEY=replace-this-with-a-random-secret-min-32-chars!!
JWT_ISSUER=MotoAdvisorAPI
JWT_AUDIENCE=MotoAdvisorClient
JWT_EXPIRES_MINUTES=60

# ── CORS ────────────────────────────────────────────────────────────────────
CORS_ORIGIN=https://motoadvisor.student-dev.ro

# ── Gemini AI ────────────────────────────────────────────────────────────────
# Optional. Leave empty to disable AI features.
GEMINI_API_KEY=
